{
  "name": "@plugg/core",
  "version": "0.0.0",
  "private": true,
  "description": "Internal: Principal shape, AuthAdapter port, typed auth errors, and canonical Principal serialization. Published as part of the `plugg` package.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
